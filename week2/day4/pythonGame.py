#imported pygame and it's modules
import pygame
from pygame.locals import * #allows us to use modules without adding pygame.locals prefix

#need this line to use pygame library
pygame.init()

#Game loop begins
while True:
      # Code
      # Code
      .
      .
      pygame.display.update()